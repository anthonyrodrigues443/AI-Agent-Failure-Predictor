numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
joblib>=1.3
pyarrow>=14.0          # parquet engine for data/processed/agent_runs.parquet

# Phase 2+ (tree ensembles)
xgboost>=2.0
lightgbm>=4.1
catboost>=1.2

# Phase 4 (hyperparameter optimization) + Phase 5 (imbalance ablation)
optuna>=3.5
imbalanced-learn>=0.12   # SMOTE / ADASYN / BorderlineSMOTE / SMOTETomek / RandomUnderSampler
# Phase 6 (explainability / production pipeline / Streamlit dashboard)
shap>=0.44
streamlit>=1.30
plotly>=5.18            # risk gauge + early-window timeline + SHAP charts in app.py
pytest>=7.4            # tests/ contract + inference + HTTP suite (47 tests)

# Phase 7 (serving)
fastapi>=0.110         # src/serve.py HTTP scoring service
uvicorn>=0.27          # ASGI server for the FastAPI app / Docker entrypoint
httpx>=0.27            # fastapi.testclient dependency (tests/test_serve.py)
